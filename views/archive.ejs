<%- include header %>
<div class="container">
	<div class="row">
		<!-- 左侧 -->
		<div class="col-xs-9">
			<!-- 定位 -->
			<%= include location %>
			<div class="jumbotron index-post-info">
				<ul class="archive">
					<% var lastYear = 0 %> 		<!-- 设定一个变量，用来判断是否和上次已经显示的年份相同，相同的不在显示年份，不通则显示 -->
					<% posts.forEach(function (post, index) { %>
						<% if (lastYear != post.time.year) { %>
							<li><h3><%= post.time.year %></h3></li>
						<% lastYear = post.time.year } %> 		<!-- 通过这句来重新赋值lastYear，从而进行比较 -->
						<li><a href="/u/<%= post.name %>/<%= post.day %>/<%= post.title %>"><%= post.title %></a><span class='archive-time'><%= post.time.day %></span></li>
					<% }) %>
				</ul>
			</div>
		</div>
		<!-- 右侧 -->
		<div class="col-xs-3">
			<!-- 本站搜索 -->
			<div class="input-group">
				<form action="/search" method="get">
					<input type="text" name="key" class="form-control" placeholder="输入要查找的内容">
					<!-- <span class="input-group-btn">
						<button class="btn btn-reverse" type="button">Go!</button>
					</span> -->
				</form>		
			</div>
			<!-- 近期文章 -->
			<div class="sb-box">
				<h4 class="sb-line">近期文章</h4>
				<ul class="sb-cont cf">
				<% posts.forEach(function(post, index) { %>
				<% var count = 4 %>
					<% if (count--) { %>
						<li>
							<a href="u/<%= post.name %>/<%= post.time.day %>/<%= post.title %>" title="<%= post.title %>" class="sb-post-title"><%= post.title %></a>
							<span class="post-data"><%= post.time.day %></span>
						</li>
					<% } %>
				<% }) %>

					<!-- <li>
						<a href="###">js 如何实现暂停和继续功能</a>
						<span class="post-data">2015-1-16</span>
					</li>
					<li>
						<a href="###">为什么php中没有ruby的webrick？</a>
						<span class="post-data">2015-1-16</span>
					</li>
					<li>
						<a href="###">php区别电脑浏览器和手机浏览器</a>
						<span class="post-data">2015-1-16</span>
					</li>
					<li>
						<a href="###">求助如何设计大数据的课程</a>
						<span class="post-data">2015-1-16</span>
					</li>
					<li>
						<a href="###">调整php-fpm进程数之后，为什么负载飙升的厉害</a>
						<span class="post-data">2015-1-16</span>
					</li> -->
				</ul>
			</div>
		<%- include sidebar %>
	</div>
</div>
<%- include footer %>